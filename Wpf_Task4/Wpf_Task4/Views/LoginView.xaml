<!-- Login window view -->
<Window x:Class="Wpf_Task4.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource LogInAcc}" 
        Height="400" 
        Width="400">

    <StackPanel Margin="20" VerticalAlignment="Center">
        <!-- Language selection buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="RU" 
                    Command="{Binding Source={StaticResource LangVM}, Path=SetLangRuCommand}"
                    Width="50" Margin="2"/>
            <Button Content="EN" 
                    Command="{Binding Source={StaticResource LangVM}, Path=SetLangEnCommand}"
                    Width="50" Margin="2"/>
            <Button Content="BE" 
                    Command="{Binding Source={StaticResource LangVM}, Path=SetLangBeCommand}"
                    Width="50" Margin="2"/>
        </StackPanel>

        <!-- Login credentials input -->
        <TextBlock Text="{DynamicResource Login}"/>
        <TextBox Text="{Binding Login}" Margin="0,5"/>

        <TextBlock Text="{DynamicResource Password}"/>
        <PasswordBox x:Name="PasswordBox" Margin="0,5"/>

        <!-- Login button with password as command parameter -->
        <Button Content="{DynamicResource LogInAcc}"
                Click="LoginButton_Click"
                CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"
                Margin="0,10"/>
    </StackPanel>
</Window>